# Gemma Native Source Ownership Snapshot

Date: 2026-07-10

Local source commits:
- WKVM: `2d09a5908f80a64d82c97778f018daaf6994a2f6`
- vLLM: `e12b91b032daed2afc34d77cca20902cef957b3c`
- SGLang: `9588cacaa1894e28dcdda937f49f50608fd0eb1f`

This is a source-code comparison, not a throughput claim. The benchmark evidence for the current no-HF WKVM path is still the same-shape report at `/run/media/xiaol/B214449214445C0B/wkvm_bench/results/gemma_native_vllm_same_shape_nohf_report_20260710.md`: WKVM native B8 `224.188 tok/s` versus vLLM B8 `240.302 tok/s`.

| Concern | vLLM source ownership | SGLang source ownership | WKVM current ownership | Current conclusion |
|---|---|---|---|---|
| Output projection | `Gemma4Attention` constructs and applies `o_proj` in `vllm/model_executor/models/gemma4.py:419` and `:543`. | Gemma model attention constructs and applies `o_proj` in `sglang/srt/models/gemma4_causal.py:335` and `:509`. | `NativeGemma4Attention` snapshots and applies `o_proj` in `wkvm/runner/gemma_native_forward.py:1701` and `:1843`. | Comparable boundary: keep `o_proj` model-side, not inside the dispatch backend. |
| Shared-KV policy | `Gemma4Attention` computes shared-layer target metadata in `vllm/model_executor/models/gemma4.py:457` and passes it to generic `Attention` at `:492`. Generic worker/cache helpers skip shared-layer KV specs and alias cache storage in `vllm/v1/worker/gpu/attn_utils.py:50`, `:71`, `:286`, and `:383`. | Gemma4 model code computes shared/source layer id in `sglang/srt/models/gemma4_causal.py:363`, passes it to `RadixAttention.layer_id` at `:394`, and uses `k/v=None` plus `save_kv_cache=False` around `:423`, `:469`, and `:501`. | `NativeGemma4Attention` keeps the policy in `_NativeAttentionMeta`, while `NativeGemma4SharedKVState` owns the HF-compatible dictionary load/store in `wkvm/runner/gemma_native_forward.py:1541`. | Partially comparable: WKVM now separates the shared-KV handoff from projection math, but still uses an HF-style shared-KV dictionary rather than source-layer-id cache aliasing. |
| Generic attention wrapper | vLLM `Attention` selects the backend in `vllm/model_executor/layers/attention/attention.py:348`, owns KV-cache spec/update decisions around `:541`, `:616`, and dispatches backend forward around `:811`. | `RadixAttention.forward` calls the active backend from forward context in `sglang/srt/layers/radix_attention.py:145`; backend selection is set by runner/registry in `sglang/srt/model_executor/model_runner.py:2475` and `sglang/srt/layers/attention/attention_registry.py:23`. | `NativeGemma4AttentionBackend` resolves token-pool calls, gates dense-cache update, and calls `_attention_forward` in `wkvm/runner/gemma_native_forward.py:1580`. | Similar shape, narrower scope: WKVM has a local wrapper, not a general engine-wide `Attention` protocol with cache specs and backend registry. |
| KV-pool write and kernel dispatch | vLLM custom op dispatches KV update and backend forward in `vllm/model_executor/layers/attention/attention.py:769` and `:811`; Triton backend writes/reads cache in `vllm/v1/attention/backends/triton_attn.py:560` and `:767`. | `RadixAttention` forwards `save_kv_cache` to the backend; FlashInfer/Triton/Torch-native backends write `token_to_kv_pool` in `sglang/srt/layers/attention/flashinfer_backend.py:976`, `sglang/srt/layers/attention/triton_backend.py:1152`, and `sglang/srt/layers/attention/torch_native_backend.py:298`. | `TokenPoolAttentionBackend` owns current-KV write ordering and reference/Triton fallback in `wkvm/runner/gemma_token_pool_attention.py:358`; `TokenPoolTritonAttentionBackend` owns flat/paged/split launch accounting at `:513`. | Source direction matches: low-level token-pool write and kernel dispatch are in backend-shaped token-pool modules, but graph metadata lifecycle and dense materialized fallback are still split across engine/native-forward code. |
| No-HF measurement boundary | Incumbents are native engine stacks by design. | Incumbents are native engine stacks by design. | Report guard requires `uses_hf_transformer_forward=false`, `uses_hf_model_construction=false`, and `native_gemma_checkpoint_loader=true`; see `experiments/gemma_bench_report.py:84`. | WKVM can now prove no HF model construction or HF decoder/attention forward in successful native rows, but it still uses Transformers-adjacent config/tokenizer/helper code around the measured path. |

Current comparable claim:

WKVM is comparable enough to report same-shape native Gemma throughput against vLLM with an explicit no-HF guard, and the model/attention source boundaries now resemble the incumbent shape: model attention owns projection/norm/RoPE/output projection, while a backend-shaped path owns cache update/write and attention dispatch. It is not yet comparable as a full vLLM/SGLang-class inference engine because WKVM still lacks their generalized attention backend protocol, cache-spec/admission machinery, source-layer KV aliasing, quantization/sharding/tensor parallel loader paths, and mature scheduler/runtime features.
